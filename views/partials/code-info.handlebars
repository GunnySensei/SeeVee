<section class="content-card flex-row">
  <div class="code">
    <a href="{{code_url}}" target="_blank">
      <h3 class="black-color">{{title}}</h3>
    </a>
    <p>Submitted by:
      {{user.username}}
      | on:
      {{createdAt}}
    </p>
  </div>
</section>